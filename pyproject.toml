[project]
name = "brave-byom-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv~=0.9.9",
    "fastapi[standard]~=0.116.2",
    "httpx~=0.28.1",
]
